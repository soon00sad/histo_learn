# Copy to .env and fill in real values before any non-development deployment.
# docker-compose.yml reads these automatically if a .env file is present.

HISTOVISION_JWT_SECRET=change-me-to-a-random-64-character-string
HISTOVISION_SEED_USER_PASSWORD=change-me
